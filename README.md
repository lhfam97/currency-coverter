# 📊 Currency Converter CLI

A robust command-line utility built in **Go** that converts currency values using local exchange rate data stored in a JSON file.

## 🚀 Features

- **Efficient Execution:** Leverages Go's compiled nature for near-instant calculations.
- **Dynamic Currency Mapping:** Uses a flexible `map` structure to support any number of currencies without requiring code changes.
- **Robust Validation:** Comprehensive checks for argument counts, numeric formatting, and currency existence.
- **Case Insensitive:** Automatically handles currency codes regardless of user input casing (e.g., `usd` vs `USD`).
- **Smart Feedback:** Alphabetically lists all available currency codes if the user provides an invalid one.

## 📋 Prerequisites

- [Go](https://go.dev/doc/install) 1.21 or higher.
- A `rates.json` file in the application directory.

## 🛠️ Installation & Build

1. **Clone the repository:**
   ```powershell
   git clone [https://github.com/lhfam97/currency-coverter.git](https://github.com/lhfam97/currency-coverter.git)
   cd currency-coverter
   ```
2. **Compile the binary:**
   ```powershell
    go build -o convert.exe main.go
   ```

## 📖 Usage

The program requires two arguments: the amount to convert and the target currency code.

Example:
.\convert.exe 100 USD

Example Output:
15.10

## 🏗️ Technical Decisions (Senior View)

- Error Handling: Avoided panic in favor of idiomatic Go error returns, providing a cleaner UX and better maintainability.
- Performance: Used map[string]float64 for O(1) lookup complexity, ensuring the tool remains fast regardless of the number of currencies.
- I/O Strategy: Encapsulated file reading into a dedicated function with error wrapping, following Go's best practices.

## 📁 JSON Data Format

The application expects a rates.json file in the same directory:

{
  "base": "BRL",
  "date": "2025-04-14",
  "rates": {
    "USD": 0.151,
    "EUR": 0.137,
    "JPY": 16.29
  }
}

---
**🚀 Generated by [Luís Henrique Machado](https://github.com/lhfam97)**

